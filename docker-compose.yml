services:
  postgres:
    image: postgres:16
    container_name: best-agent-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: best_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./table.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d best_agent"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
